# Core ML Libraries (using CPU-compatible versions for Docker)
torch>=2.0.0
transformers>=4.30.0
sentence-transformers>=2.2.0

# Data Processing
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.2.0

# NLP/Tokenization
tokenizers>=0.13.0
sentencepiece>=0.1.99

# Supporting
huggingface-hub>=0.14.0
requests>=2.28.0
tqdm>=4.65.0
networkx>=3.0
safetensors>=0.3.0
PyYAML>=6.0

# Note: MLX is macOS-only, so we use torch for cross-platform compatibility
